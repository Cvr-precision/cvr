<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#05080d">
    <meta name="color-scheme" content="dark">
    <meta name="robots" content="index, follow">
    <meta name="author" content="CVR PRECISION">
    <meta name="description" content="CVR PRECISION rachète et vend vos machines-outils, groupes électrogènes, matériels de manutention, engins, véhicules professionnels, carbure et équipements industriels partout en France.">
    <meta name="keywords" content="rachat matériel industriel, vente machines outils, rachat machines industrielles, machines occasion, groupes électrogènes occasion, matériel manutention, carbure usagé, CVR PRECISION, Le Vésinet">
    <meta property="og:type" content="website">
    <meta property="og:locale" content="fr_FR">
    <meta property="og:site_name" content="CVR PRECISION">
    <meta property="og:title" content="CVR PRECISION | Rachat et vente de matériels industriels">
    <meta property="og:description" content="Une solution rapide, simple et sécurisée pour valoriser vos équipements industriels d'occasion partout en France.">
    <meta property="og:image" content="logo.png">
    <meta property="og:image:alt" content="Logo CVR PRECISION">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="CVR PRECISION | Rachat de matériels industriels">
    <meta name="twitter:description" content="Estimation rapide, achat en l'état, paiement avant enlèvement et intervention dans toute la France.">
    <meta name="twitter:image" content="logo.png">
    <title>CVR PRECISION | Rachat et vente de matériels industriels</title>
  
<script src="js/script.js"></script>

</body>
</html>
<link rel="icon" type="image/png" href="logo.png">
<link rel="apple-touch-icon" href="logo.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preconnect" href="https://cdnjs.cloudflare.com">
<link rel="preconnect" href="https://images.unsplash.com">
<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWixAgWMJ2H8fJ9IuSc+M1s0w8YdKx6q+Vf5j8q3RJ8s8n3qV7Kx1xM1Q9z9N9m5G5h5g==" crossorigin="anonymous" referrerpolicy="no-referrer">

<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "@id": "#cvr-precision",
    "name": "CVR PRECISION",
    "image": "logo.png",
    "logo": "logo.png",
    "description": "Entreprise spécialisée dans le rachat et la vente de matériels industriels d'occasion, machines-outils, groupes électrogènes, matériels de manutention, engins, véhicules professionnels et carbure usagé.",
    "telephone": "+33695527388",
    "email": "cvr.precision@gmail.com",
    "address": {
        "@type": "PostalAddress",
        "streetAddress": "131 Boulevard Carnot",
        "postalCode": "78110",
        "addressLocality": "Le Vésinet",
        "addressCountry": "FR"
    },
    "areaServed": {
        "@type": "Country",
        "name": "France"
    },
    "contactPoint": {
        "@type": "ContactPoint",
        "telephone": "+33695527388",
        "contactType": "customer service",
        "availableLanguage": "French"
    },
    "serviceType": [
        "Rachat de matériel industriel",
        "Vente de machines industrielles d'occasion",
        "Estimation de machines-outils",
        "Enlèvement et transport de matériel industriel",
        "Rachat de carbure usagé"
    ]
}
</script>

<style>
    :root {
        --black: #030508;
        --black-soft: #070b11;
        --anthracite: #0d131c;
        --anthracite-light: #151d29;
        --steel: #8d99a8;
        --steel-light: #c8d1dc;
        --white: #f7f9fc;
        --muted: #9da9b8;
        --blue: #168cff;
        --blue-light: #5cb6ff;
        --blue-dark: #0057bd;
        --cyan: #57dcff;
        --success: #35d79a;
        --border: rgba(255, 255, 255, 0.1);
        --border-blue: rgba(22, 140, 255, 0.34);
        --glass: rgba(12, 18, 27, 0.68);
        --glass-light: rgba(255, 255, 255, 0.055);
        --shadow: 0 30px 80px rgba(0, 0, 0, 0.38);
        --shadow-blue: 0 20px 60px rgba(0, 112, 255, 0.2);
        --radius-sm: 14px;
        --radius-md: 22px;
        --radius-lg: 32px;
        --container: 1220px;
        --header-height: 86px;
        --transition: 260ms cubic-bezier(.22, .61, .36, 1);
    }

    *,
    *::before,
    *::after {
        box-sizing: border-box;
    }

    html {
        scroll-behavior: smooth;
        scroll-padding-top: calc(var(--header-height) + 24px);
        background: var(--black);
    }

    body {
        margin: 0;
        min-width: 320px;
        overflow-x: hidden;
        color: var(--white);
        background:
            radial-gradient(circle at 10% 10%, rgba(0, 113, 255, 0.14), transparent 28rem),
            radial-gradient(circle at 92% 36%, rgba(50, 204, 255, 0.08), transparent 30rem),
            linear-gradient(180deg, #030508 0%, #070b11 35%, #05080d 100%);
        font-family: "Manrope", sans-serif;
        font-size: 16px;
        line-height: 1.65;
        text-rendering: optimizeLegibility;
        -webkit-font-smoothing: antialiased;
    }

    body.menu-open {
        overflow: hidden;
    }

    ::selection {
        color: #fff;
        background: rgba(22, 140, 255, 0.75);
    }

    img {
        display: block;
        max-width: 100%;
    }

    a {
        color: inherit;
        text-decoration: none;
    }

    button,
    input,
    textarea,
    select {
        font: inherit;
    }

    button,
    a {
        -webkit-tap-highlight-color: transparent;
    }

    section {
        position: relative;
        padding: 110px 0;
    }

    .container {
        width: min(calc(100% - 40px), var(--container));
        margin-inline: auto;
    }

    .skip-link {
        position: fixed;
        top: -100px;
        left: 20px;
        z-index: 9999;
        padding: 12px 18px;
        color: #fff;
        background: var(--blue);
        border-radius: 0 0 12px 12px;
        transition: top var(--transition);
    }

    .skip-link:focus {
        top: 0;
    }

    .site-header {
        position: fixed;
        inset: 0 0 auto;
        z-index: 1000;
        height: var(--header-height);
        border-bottom: 1px solid transparent;
        transition:
            height var(--transition),
            background var(--transition),
            border-color var(--transition),
            box-shadow var(--transition);
    }

    .site-header.scrolled {
        height: 72px;
        background: rgba(3, 5, 8, 0.84);
        border-color: rgba(255, 255, 255, 0.08);
        box-shadow: 0 14px 50px rgba(0, 0, 0, 0.28);
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
    }

    .header-inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        height: 100%;
        gap: 32px;
    }

    .brand {
        display: inline-flex;
        align-items: center;
        flex-shrink: 0;
        min-width: 0;
    }

    .brand img {
        width: auto;
        height: 53px;
        object-fit: contain;
        filter: drop-shadow(0 8px 18px rgba(0, 115, 255, 0.2));
        transition: height var(--transition), transform var(--transition);
    }

    .site-header.scrolled .brand img {
        height: 45px;
    }

    .brand:hover img {
        transform: translateY(-2px);
    }

    .main-nav {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .nav-link {
        position: relative;
        padding: 11px 12px;
        color: #c9d2dd;
        font-size: 0.87rem;
        font-weight: 700;
        letter-spacing: -0.01em;
        transition: color var(--transition);
    }

    .nav-link::after {
        content: "";
        position: absolute;
        right: 12px;
        bottom: 5px;
        left: 12px;
        height: 2px;
        background: linear-gradient(90deg, transparent, var(--blue), var(--cyan), transparent);
        transform: scaleX(0);
        transform-origin: center;
        transition: transform var(--transition);
    }

    .nav-link:hover,
    .nav-link.active {
        color: #fff;
    }

    .nav-link:hover::after,
    .nav-link.active::after {
        transform: scaleX(1);
    }

    .header-cta {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 9px;
        min-height: 46px;
        padding: 0 18px;
        color: #fff;
        background: linear-gradient(135deg, var(--blue), #0067d8);
        border: 1px solid rgba(113, 194, 255, 0.54);
        border-radius: 999px;
        box-shadow: 0 12px 35px rgba(0, 116, 255, 0.28);
        font-size: 0.86rem;
        font-weight: 800;
        white-space: nowrap;
        transition:
            transform var(--transition),
            box-shadow var(--transition),
            background var(--transition);
    }

    .header-cta:hover {
        transform: translateY(-2px);
        background: linear-gradient(135deg, #37a4ff, var(--blue));
        box-shadow: 0 18px 42px rgba(0, 126, 255, 0.38);
    }

    .menu-toggle {
        display: none;
        position: relative;
        width: 48px;
        height: 48px;
        padding: 0;
        color: #fff;
        background: rgba(255, 255, 255, 0.06);
        border: 1px solid var(--border);
        border-radius: 14px;
        cursor: pointer;
    }

    .menu-toggle span {
        position: absolute;
        left: 13px;
        width: 20px;
        height: 2px;
        background: currentColor;
        border-radius: 99px;
        transition: transform var(--transition), opacity var(--transition), top var(--transition);
    }

    .menu-toggle span:nth-child(1) {
        top: 16px;
    }

    .menu-toggle span:nth-child(2) {
        top: 23px;
    }

    .menu-toggle span:nth-child(3) {
        top: 30px;
    }

    .menu-toggle.active span:nth-child(1) {
        top: 23px;
        transform: rotate(45deg);
    }

    .menu-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .menu-toggle.active span:nth-child(3) {
        top: 23px;
        transform: rotate(-45deg);
    }

    .hero {
        display: flex;
        align-items: center;
        min-height: 100svh;
        padding: calc(var(--header-height) + 70px) 0 80px;
        isolation: isolate;
        overflow: hidden;
    }

    .hero-background {
        position: absolute;
        inset: 0;
        z-index: -4;
    }

    .hero-background::before {
        content: "";
        position: absolute;
        inset: 0;
        background:
            linear-gradient(90deg, rgba(3, 5, 8, 0.98) 0%, rgba(3, 5, 8, 0.88) 38%, rgba(3, 5, 8, 0.56) 70%, rgba(3, 5, 8, 0.78) 100%),
            linear-gradient(180deg, rgba(3, 5, 8, 0.12), #030508 98%);
        z-index: 1;
    }

    .hero-background img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: 60% center;
        filter: saturate(0.78) contrast(1.08);
        transform: scale(1.03);
    }

    .hero-grid {
        position: absolute;
        inset: 0;
        z-index: -2;
        opacity: 0.24;
        background-image:
            linear-gradient(rgba(82, 179, 255, 0.09) 1px, transparent 1px),
            linear-gradient(90deg, rgba(82, 179, 255, 0.09) 1px, transparent 1px);
        background-size: 70px 70px;
        mask-image: linear-gradient(to bottom, black, transparent 92%);
        -webkit-mask-image: linear-gradient(to bottom, black, transparent 92%);
    }

    .hero-glow {
        position: absolute;
        z-index: -1;
        width: 42rem;
        height: 42rem;
        border-radius: 50%;
        background: rgba(0, 119, 255, 0.18);
        filter: blur(100px);
        pointer-events: none;
        animation: glowFloat 8s ease-in-out infinite alternate;
    }

    .hero-glow.one {
        top: -18rem;
        left: -15rem;
    }

    .hero-glow.two {
        right: -20rem;
        bottom: -21rem;
        background: rgba(70, 218, 255, 0.12);
        animation-delay: -3s;
    }

    @keyframes glowFloat {
        to {
            transform: translate3d(40px, 30px, 0) scale(1.08);
        }
    }

    .hero-content {
        max-width: 900px;
    }

    .eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 24px;
        padding: 9px 14px;
        color: #bfe3ff;
        background: rgba(6, 53, 96, 0.35);
        border: 1px solid rgba(67, 169, 255, 0.3);
        border-radius: 999px;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
        font-size: 0.76rem;
        font-weight: 800;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
    }

    .eyebrow-dot {
        width: 8px;
        height: 8px;
        background: var(--blue-light);
        border-radius: 50%;
        box-shadow: 0 0 0 5px rgba(48, 165, 255, 0.12), 0 0 18px rgba(48, 165, 255, 0.9);
        animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
        50% {
            box-shadow: 0 0 0 9px rgba(48, 165, 255, 0), 0 0 24px rgba(48, 165, 255, 1);
        }
    }

    .hero h1 {
        max-width: 960px;
        margin: 0;
        font-family: "Rajdhani", sans-serif;
        font-size: clamp(3.5rem, 7.6vw, 7rem);
        line-height: 0.92;
        font-weight: 700;
        letter-spacing: -0.055em;
        text-wrap: balance;
    }

    .hero h1 .gradient-text {
        display: block;
        color: transparent;
        background: linear-gradient(110deg, #fff 8%, #b8ddff 42%, #198eff 75%, #7de5ff 100%);
        background-clip: text;
        -webkit-background-clip: text;
        filter: drop-shadow(0 14px 35px rgba(0, 117, 255, 0.18));
    }

    .hero-subtitle {
        max-width: 720px;
        margin: 28px 0 0;
        color: #c5cfda;
        font-size: clamp(1.05rem, 2vw, 1.3rem);
        line-height: 1.7;
        text-wrap: balance;
    }

    .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        margin-top: 38px;
    }

    .button {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 11px;
        min-height: 56px;
        padding: 0 25px;
        overflow: hidden;
        border: 1px solid transparent;
        border-radius: 14px;
        font-weight: 800;
        letter-spacing: -0.01em;
        transition:
            transform var(--transition),
            border-color var(--transition),
            box-shadow var(--transition),
            background var(--transition),
            color var(--transition);
    }

    .button::before {
        content: "";
        position: absolute;
        top: -90%;
        left: -50%;
        width: 40%;
        height: 280%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.34), transparent);
        transform: rotate(24deg) translateX(-180%);
        transition: transform 700ms ease;
    }

    .button:hover::before {
        transform: rotate(24deg) translateX(520%);
    }

    .button:hover {
        transform: translateY(-3px);
    }

    .button-primary {
        color: #fff;
        background: linear-gradient(135deg, #229bff 0%, #0062d0 100%);
        border-color: rgba(126, 205, 255, 0.62);
        box-shadow: 0 18px 48px rgba(0, 112, 255, 0.34), inset 0 1px 0 rgba(255, 255, 255, 0.24);
    }

    .button-primary:hover {
        box-shadow: 0 24px 55px rgba(0, 126, 255, 0.46), inset 0 1px 0 rgba(255, 255, 255, 0.28);
    }

    .button-secondary {
        color: #f8fbff;
        background: rgba(255, 255, 255, 0.07);
        border-color: rgba(255, 255, 255, 0.18);
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
    }

    .button-secondary:hover {
        background: rgba(255, 255, 255, 0.12);
        border-color: rgba(106, 190, 255, 0.45);
    }

    .hero-trust {
        display: flex;
        flex-wrap: wrap;
        gap: 12px 26px;
        margin-top: 42px;
        color: #b7c0cb;
        font-size: 0.87rem;
        font-weight: 700;
    }

    .hero-trust span {
        display: inline-flex;
        align-items: center;
        gap: 9px;
    }

    .hero-trust i {
        color: var(--blue-light);
    }

    .scroll-indicator {
        position: absolute;
        bottom: 26px;
        left: 50%;
        z-index: 2;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 9px;
        color: rgba(255, 255, 255, 0.62);
        font-size: 0.68rem;
        font-weight: 700;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        transform: translateX(-50%);
    }

    .mouse {
        position: relative;
        width: 26px;
        height: 42px;
        border: 2px solid rgba(255, 255, 255, 0.32);
        border-radius: 20px;
    }

    .mouse::after {
        content: "";
        position: absolute;
        top: 8px;
        left: 50%;
        width: 4px;
        height: 8px;
        background: var(--blue-light);
        border-radius: 99px;
        transform: translateX(-50%);
        animation: scrollMouse 1.8s ease infinite;
    }

    @keyframes scrollMouse {
        0% {
            opacity: 0;
            transform: translate(-50%, 0);
        }

        30% {
            opacity: 1;
        }

        100% {
            opacity: 0;
            transform: translate(-50%, 14px);
        }
    }

    .section-label {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 18px;
        color: var(--blue-light);
        font-size: 0.76rem;
        font-weight: 800;
        letter-spacing: 0.18em;
        text-transform: uppercase;
    }

    .section-label::before {
        content: "";
        width: 34px;
        height: 2px;
        background: linear-gradient(90deg, var(--blue), var(--cyan));
        border-radius: 99px;
        box-shadow: 0 0 12px rgba(22, 140, 255, 0.7);
    }

    .section-heading {
        max-width: 840px;
        margin: 0;
        font-family: "Rajdhani", sans-serif;
        font-size: clamp(2.5rem, 5vw, 4.7rem);
        line-height: 1;
        font-weight: 700;
        letter-spacing: -0.045em;
        text-wrap: balance;
    }

    .section-heading span {
        color: var(--blue-light);
    }

    .section-intro {
        max-width: 720px;
        margin: 22px 0 0;
        color: var(--muted);
        font-size: 1.04rem;
    }

    .about {
        overflow: hidden;
    }

    .about::before {
        content: "";
        position: absolute;
        top: 9%;
        right: -15rem;
        width: 34rem;
        height: 34rem;
        background: rgba(0, 111, 255, 0.1);
        border-radius: 50%;
        filter: blur(100px);
        pointer-events: none;
    }

    .about-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr) minmax(380px, 0.82fr);
        align-items: center;
        gap: 74px;
    }

    .about-copy p {
        margin: 24px 0 0;
        color: #aab5c2;
        font-size: 1.02rem;
    }

    .about-copy strong {
        color: #fff;
    }

    .about-points {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 14px;
        margin-top: 34px;
    }

    .mini-point {
        display: flex;
        align-items: center;
        gap: 12px;
        min-height: 70px;
        padding: 16px;
        background: linear-gradient(145deg, rgba(255, 255, 255, 0.055), rgba(255, 255, 255, 0.025));
        border: 1px solid var(--border);
        border-radius: 16px;
        transition:
            transform var(--transition),
            border-color var(--transition),
            background var(--transition);
    }

    .mini-point:hover {
        transform: translateY(-4px);
        background: rgba(22, 140, 255, 0.07);
        border-color: var(--border-blue);
    }

    .mini-point i {
        display: grid;
        place-items: center;
        flex: 0 0 38px;
        height: 38px;
        color: var(--blue-light);
        background: rgba(22, 140, 255, 0.12);
        border: 1px solid rgba(54, 163, 255, 0.22);
        border-radius: 11px;
    }

    .mini-point span {
        color: #e6ebf1;
        font-size: 0.88rem;
        font-weight: 750;
    }

    .about-visual {
        position: relative;
        min-height: 580px;
    }

    .about-image {
        position: absolute;
        overflow: hidden;
        background: #0b1119;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 26px;
        box-shadow: var(--shadow);
    }

    .about-image::after {
        content: "";
        position: absolute;
        inset: 0;
        background: linear-gradient(180deg, transparent 50%, rgba(3, 5, 8, 0.72));
        pointer-events: none;
    }

    .about-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 800ms cubic-bezier(.22, .61, .36, 1);
    }

    .about-image:hover img {
        transform: scale(1.06);
    }

    .about-image.large {
        top: 0;
        right: 0;
        width: 82%;
        height: 76%;
    }

    .about-image.small {
        bottom: 0;
        left: 0;
        z-index: 2;
        width: 58%;
        height: 44%;
        border: 7px solid #070b11;
    }

    .floating-stat {
        position: absolute;
        right: -18px;
        bottom: 48px;
        z-index: 3;
        min-width: 218px;
        padding: 23px;
        background: rgba(8, 14, 23, 0.88);
        border: 1px solid rgba(64, 172, 255, 0.32);
        border-radius: 19px;
        box-shadow: var(--shadow-blue);
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
    }

    .floating-stat strong {
        display: block;
        color: #fff;
        font-family: "Rajdhani", sans-serif;
        font-size: 2.2rem;
        line-height: 1;
    }

    .floating-stat span {
        display: block;
        margin-top: 6px;
        color: var(--muted);
        font-size: 0.78rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .categories {
        background:
            linear-gradient(180deg, rgba(255, 255, 255, 0.012), transparent),
            radial-gradient(circle at 50% 0%, rgba(22, 140, 255, 0.09), transparent 38rem);
    }

    .categories-head {
        display: flex;
        align-items: end;
        justify-content: space-between;
        gap: 40px;
        margin-bottom: 48px;
    }

    .categories-head .section-intro {
        max-width: 430px;
        margin: 0 0 6px;
    }

    .category-grid {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        gap: 16px;
    }

    .category-card {
        position: relative;
        display: flex;
        align-items: center;
        gap: 15px;
        min-height: 108px;
        padding: 19px;
        overflow: hidden;
        background:
            linear-gradient(145deg, rgba(255, 255, 255, 0.065), rgba(255, 255, 255, 0.018)),
            #0a0f16;
        border: 1px solid var(--border);
        border-radius: 18px;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
        transition:
            transform var(--transition),
            border-color var(--transition),
            background var(--transition),
            box-shadow var(--transition);
    }

    .category-card::before {
        content: "";
        position: absolute;
        top: -70px;
        right: -70px;
        width: 120px;
        height: 120px;
        background: rgba(22, 140, 255, 0.15);
        border-radius: 50%;
        filter: blur(30px);
        opacity: 0;
        transition: opacity var(--transition), transform var(--transition);
    }

    .category-card::after {
        content: "";
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
        height: 2px;
        background: linear-gradient(90deg, transparent, var(--blue), var(--cyan), transparent);
        transform: scaleX(0);
        transition: transform var(--transition);
    }

    .category-card:hover {
        transform: translateY(-7px);
        background:
            linear-gradient(145deg, rgba(22, 140, 255, 0.12), rgba(255, 255, 255, 0.025)),
            #0b1119;
        border-color: rgba(67, 169, 255, 0.42);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.26), 0 0 35px rgba(0, 107, 255, 0.08);
    }

    .category-card:hover::before {
        opacity: 1;
        transform: translate(-20px, 20px);
    }

    .category-card:hover::after {
        transform: scaleX(1);
    }

    .category-icon {
        position: relative;
        z-index: 1;
        display: grid;
        place-items: center;
        flex: 0 0 52px;
        height: 52px;
        color: var(--blue-light);
        background:
            linear-gradient(145deg, rgba(50, 161, 255, 0.22), rgba(0, 81, 170, 0.11));
        border: 1px solid rgba(74, 178, 255, 0.28);
        border-radius: 15px;
        font-size: 1.25rem;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
        transition: transform var(--transition), color var(--transition), box-shadow var(--transition);
    }

    .category-card:hover .category-icon {
        color: #fff;
        transform: rotate(-5deg) scale(1.06);
        box-shadow: 0 0 28px rgba(22, 140, 255, 0.22);
    }

    .category-card h3 {
        position: relative;
        z-index: 1;
        margin: 0;
        color: #e8edf3;
        font-size: 0.92rem;
        line-height: 1.35;
        font-weight: 760;
    }

    .process {
        overflow: hidden;
    }

    .process::after {
        content: "";
        position: absolute;
        top: 15%;
        left: -18rem;
        width: 40rem;
        height: 40rem;
        background: rgba(30, 143, 255, 0.08);
        border-radius: 50%;
        filter: blur(110px);
        pointer-events: none;
    }

    .process-grid {
        display: grid;
        grid-template-columns: 0.8fr 1.2fr;
        align-items: center;
        gap: 70px;
        margin-top: 54px;
    }

    .process-visual {
        position: relative;
        min-height: 660px;
        overflow: hidden;
        border: 1px solid var(--border);
        border-radius: 30px;
        box-shadow: var(--shadow);
    }

    .process-visual::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: 1;
        background: linear-gradient(180deg, transparent 30%, rgba(3, 5, 8, 0.92) 100%);
    }

    .process-visual img {
        width: 100%;
        height: 100%;
        min-height: 660px;
        object-fit: cover;
        filter: saturate(0.8);
    }

    .process-overlay {
        position: absolute;
        right: 28px;
        bottom: 28px;
        left: 28px;
        z-index: 2;
        padding: 26px;
        background: rgba(7, 12, 19, 0.72);
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 20px;
        backdrop-filter: blur(18px);
        -webkit-backdrop-filter: blur(18px);
    }

    .process-overlay strong {
        display: block;
        font-family: "Rajdhani", sans-serif;
        font-size: 1.7rem;
        line-height: 1.1;
    }

    .process-overlay p {
        margin: 10px 0 0;
        color: #aab5c2;
        font-size: 0.9rem;
    }

    .service-list {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 14px;
    }

    .service-card {
        position: relative;
        min-height: 170px;
        padding: 24px;
        overflow: hidden;
        background:
            linear-gradient(145deg, rgba(255, 255, 255, 0.06), rgba(255, 255, 255, 0.018)),
            #0a0f16;
        border: 1px solid var(--border);
        border-radius: 19px;
        transition:
            transform var(--transition),
            border-color var(--transition),
            background var(--transition),
            box-shadow var(--transition);
    }

    .service-card:hover {
        transform: translateY(-6px);
        background:
            linear-gradient(145deg, rgba(22, 140, 255, 0.11), rgba(255, 255, 255, 0.018)),
            #0a1018;
        border-color: rgba(54, 163, 255, 0.36);
        box-shadow: 0 22px 48px rgba(0, 0, 0, 0.22);
    }

    .service-number {
        position: absolute;
        top: 10px;
        right: 17px;
        color: rgba(255, 255, 255, 0.045);
        font-family: "Rajdhani", sans-serif;
        font-size: 4.3rem;
        line-height: 1;
        font-weight: 700;
    }

    .service-card i {
        display: grid;
        place-items: center;
        width: 46px;
        height: 46px;
        margin-bottom: 20px;
        color: var(--blue-light);
        background: rgba(22, 140, 255, 0.12);
        border: 1px solid rgba(67, 169, 255, 0.23);
        border-radius: 13px;
        font-size: 1.1rem;
    }

    .service-card h3 {
        position: relative;
        margin: 0;
        font-size: 1rem;
        line-height: 1.4;
    }

    .service-card p {
        position: relative;
        margin: 9px 0 0;
        color: var(--muted);
        font-size: 0.84rem;
        line-height: 1.6;
    }

    .stats-section {
        padding: 38px 0;
    }

    .stats-panel {
        display: grid;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        overflow: hidden;
        background:
            linear-gradient(135deg, rgba(22, 140, 255, 0.14), rgba(255, 255, 255, 0.035) 45%, rgba(52, 218, 255, 0.07)),
            #0a0f16;
        border: 1px solid rgba(65, 171, 255, 0.25);
        border-radius: 25px;
        box-shadow: var(--shadow-blue);
    }

    .stat-item {
        position: relative;
        padding: 35px 25px;
        text-align: center;
    }

    .stat-item:not(:last-child)::after {
        content: "";
        position: absolute;
        top: 24%;
        right: 0;
        width: 1px;
        height: 52%;
        background: linear-gradient(transparent, rgba(255, 255, 255, 0.15), transparent);
    }

    .stat-value {
        display: inline-flex;
        align-items: baseline;
        justify-content: center;
        color: #fff;
        font-family: "Rajdhani", sans-serif;
        font-size: clamp(2.5rem, 5vw, 4rem);
        line-height: 1;
        font-weight: 700;
        letter-spacing: -0.04em;
    }

    .stat-value span {
        color: var(--blue-light);
    }

    .stat-label {
        display: block;
        margin-top: 8px;
        color: #aeb9c5;
        font-size: 0.76rem;
        font-weight: 800;
        letter-spacing: 0.11em;
        text-transform: uppercase;
    }

    .why {
        overflow: hidden;
    }

    .why-layout {
        display: grid;
        grid-template-columns: minmax(0, 0.82fr) minmax(500px, 1.18fr);
        gap: 70px;
        align-items: center;
    }

    .why-copy {
        position: sticky;
        top: 110px;
    }

    .why-copy .section-intro {
        max-width: 540px;
    }

    .why-cta {
        margin-top: 30px;
    }

    .why-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 18px;
    }

    .why-card {
        position: relative;
        min-height: 250px;
        padding: 28px;
        overflow: hidden;
        background:
            linear-gradient(155deg, rgba(255, 255, 255, 0.07), rgba(255, 255, 255, 0.015)),
            #090e15;
        border: 1px solid var(--border);
        border-radius: 22px;
        transition:
            transform var(--transition),
            border-color var(--transition),
            box-shadow var(--transition);
    }

    .why-card:nth-child(even) {
        transform: translateY(26px);
    }

    .why-card:hover {
        transform: translateY(-7px);
        border-color: rgba(67, 169, 255, 0.4);
        box-shadow: 0 24px 55px rgba(0, 0, 0, 0.27), 0 0 38px rgba(0, 111, 255, 0.07);
    }

    .why-card:nth-child(even):hover {
        transform: translateY(19px);
    }

    .why-card::before {
        content: "";
        position: absolute;
        top: -75px;
        right: -65px;
        width: 150px;
        height: 150px;
        background: rgba(22, 140, 255, 0.13);
        border-radius: 50%;
        filter: blur(38px);
    }

    .why-icon {
        display: grid;
        place-items: center;
        width: 58px;
        height: 58px;
        color: #d9efff;
        background: linear-gradient(145deg, rgba(36, 157, 255, 0.25), rgba(0, 77, 161, 0.12));
        border: 1px solid rgba(82, 183, 255, 0.3);
        border-radius: 17px;
        font-size: 1.35rem;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.12);
    }

    .why-card h3 {
        margin: 23px 0 0;
        font-family: "Rajdhani", sans-serif;
        font-size: 1.5rem;
        line-height: 1.1;
    }

    .why-card p {
        margin: 12px 0 0;
        color: var(--muted);
        font-size: 0.9rem;
    }

    .gallery {
        padding-bottom: 60px;
    }

    .gallery-head {
        display: flex;
        align-items: end;
        justify-content: space-between;
        gap: 32px;
        margin-bottom: 42px;
    }

    .gallery-grid {
        display: grid;
        grid-template-columns: 1.25fr 0.75fr 0.75fr;
        grid-template-rows: 290px 250px;
        gap: 16px;
    }

    .gallery-item {
        position: relative;
        overflow: hidden;
        background: #0a0f16;
        border: 1px solid var(--border);
        border-radius: 22px;
    }

    .gallery-item:first-child {
        grid-row: 1 / 3;
    }

    .gallery-item:nth-child(4) {
        grid-column: 2 / 4;
    }

    .gallery-item::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: 1;
        background: linear-gradient(180deg, transparent 46%, rgba(3, 5, 8, 0.88));
        transition: background var(--transition);
    }

    .gallery-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: saturate(0.8);
        transition: transform 900ms cubic-bezier(.22, .61, .36, 1), filter var(--transition);
    }

    .gallery-item:hover img {
        transform: scale(1.07);
        filter: saturate(1);
    }

    .gallery-caption {
        position: absolute;
        right: 22px;
        bottom: 20px;
        left: 22px;
        z-index: 2;
    }

    .gallery-caption span {
        display: block;
        color: var(--blue-light);
        font-size: 0.7rem;
        font-weight: 800;
        letter-spacing: 0.13em;
        text-transform: uppercase;
    }

    .gallery-caption strong {
        display: block;
        margin-top: 4px;
        font-family: "Rajdhani", sans-serif;
        font-size: 1.25rem;
        line-height: 1.1;
    }

    .contact {
        padding-bottom: 0;
        overflow: hidden;
    }

    .contact::before {
        content: "";
        position: absolute;
        top: 10%;
        left: 50%;
        width: 50rem;
        height: 50rem;
        background: rgba(0, 115, 255, 0.09);
        border-radius: 50%;
        filter: blur(130px);
        transform: translateX(-50%);
        pointer-events: none;
    }

    .contact-panel {
        position: relative;
        display: grid;
        grid-template-columns: 0.86fr 1.14fr;
        overflow: hidden;
        background:
            linear-gradient(135deg, rgba(22, 140, 255, 0.12), transparent 36%),
            #090e15;
        border: 1px solid rgba(255, 255, 255, 0.11);
        border-radius: 30px;
        box-shadow: var(--shadow);
    }

    .contact-content {
        padding: 52px;
    }

    .contact-content .section-heading {
        font-size: clamp(2.5rem, 4.5vw, 4.1rem);
    }

    .contact-intro {
        margin: 19px 0 0;
        color: var(--muted);
    }

    .contact-details {
        display: grid;
        gap: 14px;
        margin-top: 34px;
    }

    .contact-card {
        display: flex;
        align-items: center;
        gap: 16px;
        min-width: 0;
        padding: 17px;
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid var(--border);
        border-radius: 17px;
        transition:
            transform var(--transition),
            border-color var(--transition),
            background var(--transition);
    }

    a.contact-card:hover {
        transform: translateX(5px);
        background: rgba(22, 140, 255, 0.08);
        border-color: rgba(67, 169, 255, 0.34);
    }

    .contact-card-icon {
        display: grid;
        place-items: center;
        flex: 0 0 49px;
        height: 49px;
        color: var(--blue-light);
        background: rgba(22, 140, 255, 0.12);
        border: 1px solid rgba(67, 169, 255, 0.24);
        border-radius: 14px;
    }

    .contact-card-body {
        min-width: 0;
    }

    .contact-card small {
        display: block;
        color: #798696;
        font-size: 0.68rem;
        font-weight: 800;
        letter-spacing: 0.12em;
        text-transform: uppercase;
    }

    .contact-card strong {
        display: block;
        margin-top: 3px;
        overflow-wrap: anywhere;
        color: #eaf0f6;
        font-size: 0.94rem;
    }

    .contact-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin-top: 28px;
    }

    .contact-actions .button {
        min-height: 52px;
        padding-inline: 20px;
        font-size: 0.88rem;
    }

    .map-wrapper {
        min-height: 100%;
        background: #0b1018;
    }

    .map-wrapper iframe {
        width: 100%;
        height: 100%;
        min-height: 670px;
        border: 0;
        filter: grayscale(0.82) invert(0.9) contrast(0.85) brightness(0.72) hue-rotate(175deg);
    }

    .site-footer {
        margin-top: 110px;
        background: #020407;
        border-top: 1px solid var(--border);
    }

    .footer-main {
        display: grid;
        grid-template-columns: 1.4fr 0.75fr 0.95fr;
        gap: 60px;
        padding: 70px 0 55px;
    }

    .footer-brand img {
        width: auto;
        height: 62px;
        object-fit: contain;
    }

    .footer-brand p {
        max-width: 440px;
        margin: 22px 0 0;
        color: #8793a2;
        font-size: 0.9rem;
    }

    .social-links {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 25px;
    }

    .social-link {
        display: grid;
        place-items: center;
        width: 43px;
        height: 43px;
        color: #cbd5df;
        background: rgba(255, 255, 255, 0.045);
        border: 1px solid var(--border);
        border-radius: 12px;
        transition:
            color var(--transition),
            background var(--transition),
            border-color var(--transition),
            transform var(--transition);
    }

    .social-link:hover {
        color: #fff;
        background: rgba(22, 140, 255, 0.14);
        border-color: rgba(67, 169, 255, 0.34);
        transform: translateY(-4px);
    }

    .footer-column h2 {
        margin: 0 0 22px;
        font-family: "Rajdhani", sans-serif;
        font-size: 1.25rem;
    }

    .footer-links {
        display: grid;
        gap: 11px;
    }

    .footer-links a,
    .footer-contact-item {
        color: #8996a5;
        font-size: 0.87rem;
        transition: color var(--transition), transform var(--transition);
    }

    .footer-links a:hover {
        color: #fff;
        transform: translateX(5px);
    }

    .footer-contact-item {
        display: flex;
        align-items: flex-start;
        gap: 10px;
        margin-bottom: 14px;
    }

    .footer-contact-item i {
        width: 18px;
        margin-top: 5px;
        color: var(--blue-light);
        text-align: center;
    }

    .footer-contact-item a:hover {
        color: #fff;
    }

    .footer-bottom {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 24px;
        padding: 22px 0 28px;
        color: #647181;
        border-top: 1px solid rgba(255, 255, 255, 0.065);
        font-size: 0.78rem;
    }

    .footer-bottom strong {
        color: #9ba7b5;
    }

    .footer-bottom-links {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
    }

    .footer-bottom-links a:hover {
        color: #fff;
    }

    .back-to-top {
        position: fixed;
        right: 24px;
        bottom: 24px;
        z-index: 800;
        display: grid;
        place-items: center;
        width: 50px;
        height: 50px;
        color: #fff;
        background: linear-gradient(145deg, #249cff, #005bbc);
        border: 1px solid rgba(129, 205, 255, 0.55);
        border-radius: 15px;
        box-shadow: 0 15px 38px rgba(0, 107, 227, 0.35);
        cursor: pointer;
        opacity: 0;
        visibility: hidden;
        transform: translateY(20px);
        transition:
            opacity var(--transition),
            visibility var(--transition),
            transform var(--transition),
            box-shadow var(--transition);
    }

    .back-to-top.visible {
        opacity: 1;
        visibility: visible;
        transform: translateY(0);
    }

    .back-to-top:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 44px rgba(0, 123, 255, 0.46);
    }

    .reveal {
        opacity: 0;
        transform: translateY(34px);
        transition:
            opacity 800ms cubic-bezier(.22, .61, .36, 1),
            transform 800ms cubic-bezier(.22, .61, .36, 1);
    }

    .reveal.from-left {
        transform: translateX(-40px);
    }

    .reveal.from-right {
        transform: translateX(40px);
    }

    .reveal.visible {
        opacity: 1;
        transform: translate3d(0, 0, 0);
    }

    .delay-1 {
        transition-delay: 80ms;
    }

    .delay-2 {
        transition-delay: 160ms;
    }

    .delay-3 {
        transition-delay: 240ms;
    }

    .delay-4 {
        transition-delay: 320ms;
    }

    @media (max-width: 1100px) {
        .main-nav {
            gap: 2px;
        }

        .nav-link {
            padding-inline: 8px;
            font-size: 0.8rem;
        }

        .header-cta {
            padding-inline: 14px;
        }

        .about-grid,
        .process-grid,
        .why-layout {
            gap: 44px;
        }

        .category-grid {
            grid-template-columns: repeat(3, minmax(0, 1fr));
        }

        .why-layout {
            grid-template-columns: minmax(0, 0.7fr) minmax(480px, 1.3fr);
        }

        .contact-content {
            padding: 42px;
        }
    }

    @media (max-width: 920px) {
        :root {
            --header-height: 76px;
        }

        section {
            padding: 90px 0;
        }

        .menu-toggle {
            display: block;
            z-index: 1002;
        }

        .header-cta {
            display: none;
        }

        .main-nav {
            position: fixed;
            inset: 0;
            z-index: 1001;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            justify-content: center;
            gap: 5px;
            padding: 110px 30px 40px;
            background:
                radial-gradient(circle at 50% 10%, rgba(22, 140, 255, 0.16), transparent 25rem),
                rgba(3, 5, 8, 0.97);
            backdrop-filter: blur(22px);
            -webkit-backdrop-filter: blur(22px);
            opacity: 0;
            visibility: hidden;
            transform: translateY(-15px);
            transition:
                opacity var(--transition),
                visibility var(--transition),
                transform var(--transition);
        }

        .main-nav.open {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .nav-link {
            padding: 14px 18px;
            color: #edf3f8;
            border-bottom: 1px solid rgba(255, 255, 255, 0.07);
            font-family: "Rajdhani", sans-serif;
            font-size: 1.45rem;
            text-align: center;
        }

        .nav-link::after {
            display: none;
        }

        .hero {
            min-height: 900px;
        }

        .hero-content {
            max-width: 780px;
        }

        .hero-background::before {
            background:
                linear-gradient(90deg, rgba(3, 5, 8, 0.96), rgba(3, 5, 8, 0.72)),
                linear-gradient(180deg, rgba(3, 5, 8, 0.1), #030508);
        }

        .about-grid,
        .process-grid,
        .why-layout,
        .contact-panel {
            grid-template-columns: 1fr;
        }

        .about-visual {
            min-height: 620px;
        }

        .categories-head,
        .gallery-head {
            display: block;
        }

        .categories-head .section-intro {
            max-width: 680px;
            margin-top: 20px;
        }

        .category-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .process-visual {
            min-height: 520px;
        }

        .process-visual img {
            min-height: 520px;
        }

        .why-copy {
            position: static;
        }

        .why-grid {
            margin-top: 10px;
        }

        .stats-panel {
            grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        .stat-item:nth-child(2)::after {
            display: none;
        }

        .stat-item:nth-child(-n+2) {
            border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        }

        .gallery-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
            grid-template-rows: 360px 260px 260px;
        }

        .gallery-item:first-child {
            grid-column: 1 / 3;
            grid-row: auto;
        }

        .gallery-item:nth-child(4) {
            grid-column: 1 / 3;
        }

        .map-wrapper iframe {
            min-height: 460px;
        }

        .footer-main {
            grid-template-columns: 1.3fr 0.7fr;
        }

        .footer-column:last-child {
            grid-column: 1 / 3;
        }
    }

    @media (max-width: 680px) {
        :root {
            --radius-lg: 24px;
        }

        .container {
            width: min(calc(100% - 28px), var(--container));
        }

        section {
            padding: 76px 0;
        }

        .brand img {
            height: 45px;
        }

        .site-header.scrolled .brand img {
            height: 40px;
        }

        .menu-toggle {
            width: 44px;
            height: 44px;
        }

        .hero {
            min-height: 830px;
            padding-top: calc(var(--header-height) + 70px);
            padding-bottom: 100px;
        }

        .hero h1 {
            font-size: clamp(3.15rem, 16vw, 5.1rem);
        }

        .hero-subtitle {
            font-size: 1rem;
        }

        .eyebrow {
            padding: 8px 12px;
            font-size: 0.65rem;
            letter-spacing: 0.12em;
        }

        .hero-actions {
            display: grid;
        }

        .hero-actions .button {
            width: 100%;
        }

        .hero-trust {
            display: grid;
            grid-template-columns: 1fr;
            gap: 9px;
        }

        .section-heading {
            font-size: clamp(2.35rem, 12vw, 3.5rem);
        }

        .about-points,
        .category-grid,
        .service-list,
        .why-grid {
            grid-template-columns: 1fr;
        }

        .about-visual {
            min-height: 500px;
        }

        .about-image.large {
            width: 92%;
            height: 70%;
        }

        .about-image.small {
            width: 68%;
            height: 39%;
        }

        .floating-stat {
            right: 0;
            bottom: 23px;
            min-width: 185px;
            padding: 18px;
        }

        .category-card {
            min-height: 94px;
        }

        .process-visual,
        .process-visual img {
            min-height: 430px;
        }

        .process-overlay {
            right: 16px;
            bottom: 16px;
            left: 16px;
            padding: 20px;
        }

        .service-card {
            min-height: 155px;
        }

        .stats-panel {
            grid-template-columns: 1fr 1fr;
        }

        .stat-item {
            padding: 27px 12px;
        }

        .stat-item::after {
            display: none;
        }

        .stat-item:nth-child(odd) {
            border-right: 1px solid rgba(255, 255, 255, 0.08);
        }

        .stat-value {
            font-size: 2.5rem;
        }

        .stat-label {
            font-size: 0.64rem;
        }

        .why-card,
        .why-card:nth-child(even),
        .why-card:nth-child(even):hover {
            min-height: 220px;
            transform: none;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: 1fr;
            grid-template-rows: repeat(4, 290px);
        }

        .gallery-item:first-child,
        .gallery-item:nth-child(4) {
            grid-column: auto;
            grid-row: auto;
        }

        .contact-panel {
            border-radius: 23px;
        }

        .contact-content {
            padding: 30px 20px;
        }

        .contact-actions {
            display: grid;
        }

        .contact-actions .button {
            width: 100%;
        }

        .map-wrapper iframe {
            min-height: 390px;
        }

        .site-footer {
            margin-top: 76px;
        }

        .footer-main {
            grid-template-columns: 1fr;
            gap: 42px;
            padding: 55px 0 45px;
        }

        .footer-column:last-child {
            grid-column: auto;
        }

        .footer-bottom {
            display: grid;
            text-align: center;
            justify-content: center;
        }

        .footer-bottom-links {
            justify-content: center;
        }

        .back-to-top {
            right: 16px;
            bottom: 16px;
            width: 46px;
            height: 46px;
        }

        .scroll-indicator {
            display: none;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        html {
            scroll-behavior: auto;
        }

        *,
        *::before,
        *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
            scroll-behavior: auto !important;
        }

        .reveal {
            opacity: 1;
            transform: none;
        }
    }
</style></body>
</html><body>
    <a class="skip-link" href="#main-content">Aller au contenu</a>
